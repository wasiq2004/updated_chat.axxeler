# Base URL of the Zen Chat MCP API (note the /api/mcp/v1 suffix).
Z_CHAT_API_URL=https://crm.srv879786.hstgr.cloud/api/mcp/v1

# A bearer key minted in Zen Chat → Admin Settings → MCP Tools (shown once).
Z_CHAT_API_KEY=zck_live_PASTE_YOUR_KEY
